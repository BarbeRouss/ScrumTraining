version: '3.4'

services:
  blazorapp.scrum:
    image: ${DOCKER_REGISTRY-}blazorappscrum
    build:
      context: .
      dockerfile: Scrum.BlazorApp/Dockerfile
    restart: always
    ports:
      - 7272:80
